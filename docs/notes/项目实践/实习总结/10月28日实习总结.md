# 今日日记

1.  有需求需要完成：

    *   内容:将后端加密的身份证数据信息进行解密并展示到页面上1.
2.  问题解决的思路和今天的收获：

    1.  解决思路：今天分配给我和另一个和我一起来的实习生四个接口，我们一人两个，需要做的就是将接口返回的数据中的加密身份信息解密，以明文的形式渲染到页面上。
    2.  解决过程：刚开始其实有点蒙蒙的，虽然这个需求很简单，也是给我用来练手的，但是作为第一次实习的我还是很懵圈，不过我还是先整理了一下思路：首先其实要明白接口返回的数据到底有些什么，然后判断数据是渲染到哪个组件的，最后找到对应的组件中请求后端数据的代码，将返回的数据先解密，再渲染，其实过程中接口访问一直超时，我只能通过运行的项目中，一个一个查看有关身份信息的界面，来判断是哪个页面，然后后面发现其实可以将接口复制在vscode中搜索，然后找到对应的组件，通过组件内的代码来判断是不是我需要修改的组件，最后也是通过ai将代码修改了，然后提交到了本地，但是其实自己并不理解为什么这么写，现在准备看一下实现的过程。
    3.  收获：也算是真正意义上接到了需求，并开始完成这个需求，从开会接需求完成需求提交本地然后测试（今天还没完成）最后提交线上合并代码，算是完整经历了一遍真实开发过程，但是还有很多不足，代码都是靠ai，希望自己在之后的实习过程中学到更多东西。

